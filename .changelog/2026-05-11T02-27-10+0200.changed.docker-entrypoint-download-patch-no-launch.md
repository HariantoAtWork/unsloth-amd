## Entrypoint: download → patch → `install.sh --no-launch`

Rewrote `docker-entrypoint.sh` so the flow is explicit: **curl** the official installer to a temp file, **`_patch_install_sh`** injects `--no-launch` + early exit before Unsloth’s interactive Studio tail, then **`sh … --no-launch </dev/null`**. README automatic-first-start text matches.
