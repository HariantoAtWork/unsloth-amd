## Add `patch.sh` for local `--no-launch` preview

Standalone script applies the same edits as `docker-entrypoint.sh` to any copy of Unsloth’s installer (e.g. `installer.sh`), writes `*.patched.sh` by default, optional `--diff` for unified diff.
