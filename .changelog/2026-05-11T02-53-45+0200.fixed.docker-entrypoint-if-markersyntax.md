## Fix `docker-entrypoint.sh` install gate `[[ … ]]`

Restored the intended condition: marker missing **and** `UNSLOTH_SKIP_AUTO_INSTALL` not `1`. A bad merge had `-f` applied to the env var and invalid `[[ … != … ]]` syntax.
