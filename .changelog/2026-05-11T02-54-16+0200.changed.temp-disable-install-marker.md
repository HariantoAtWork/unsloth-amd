## Temporarily disable install-complete marker

Entrypoint no longer checks/touches `~/.unsloth/.docker-install-complete`; Unsloth installer runs on every container start unless `UNSLOTH_SKIP_AUTO_INSTALL=1`. Comment in `docker-entrypoint.sh` documents how to restore marker behaviour.
