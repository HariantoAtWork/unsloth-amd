Stop forcing --enable-tools on autoload `studio run`. With tools enabled, GGUF /v1 chat returns SSE with tool_status even for stream=false, which breaks external OpenAI-compatible clients while the GUI still works.
