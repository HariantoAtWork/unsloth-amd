Stop using `unsloth studio run` for autoload so it no longer mints a new cli API key every reboot. Persist a stable key at ~/.unsloth/.docker-api-key and reload the last model via /api/inference/load with that key.
