Split ROCM_VERSION (apt channel, e.g. 7.2.4) from TORCH_ROCM_VERSION (PyTorch wheel tag, e.g. 7.12.0). Using 7.12 as the apt path 404s on repo.radeon.com because that numbering only exists for wheels, not apt.
