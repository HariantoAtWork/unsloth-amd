Share the Unsloth Studio venv, llama.cpp build, and helper transformers venvs across amd/planner/builder via overlay volumes, while keeping per-service `*-home` volumes for auth, studio.db, runs, and Docker markers. Entrypoint skips `install.sh` when the shared install is already usable.
