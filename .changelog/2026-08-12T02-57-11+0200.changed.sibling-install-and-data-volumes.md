Refactor volumes to sibling paths: shared `unsloth-install` at `/opt/unsloth-install`, per-service data at `/data/unsloth`, with the entrypoint rebuilding `~/.unsloth` as symlinks so install and save data no longer share one overlay tree.
