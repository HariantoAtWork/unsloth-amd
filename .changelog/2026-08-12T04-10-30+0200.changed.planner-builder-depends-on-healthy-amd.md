Make planner and builder `depends_on` unsloth-amd with `service_healthy`, and add a healthcheck that passes only after the shared install and torch pin markers exist so they do not race the first-boot installer.
