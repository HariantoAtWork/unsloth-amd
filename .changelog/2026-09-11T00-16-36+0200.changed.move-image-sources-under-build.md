Moved image sources into `build/unsloth-amd/` (Dockerfile, entrypoint, studio scripts) so additional images can live as siblings under `build/`. Runtime compose now uses the published image only; builds go through `docker-compose.build.yml` (and `bun run docker:build`).
