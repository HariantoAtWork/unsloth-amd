# Cloudflare Tunnel for the docker services
networks:
  cloudflared:
    external: true

services:
  unsloth-amd:
    networks:
      - cloudflared
  unsloth-planner:
    networks:
      - cloudflared
  unsloth-builder:
    networks:
      - cloudflared